int main() {
    string s;
    cin>>s;
    int n=s.length();
    map<char,int>mp;
    
    
    for(int i=0;i<n;i++ ){
        mp[s[i]]++;
    }
    for(auto it:mp ){
        if(it.second>1){
            cout<<it.first<<endl;
        }
    }
    return 0;
}
